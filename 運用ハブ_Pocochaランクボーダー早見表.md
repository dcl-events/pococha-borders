> 📌 最終更新: 2026-07-31（「ボーダー推移」ページを追加。クラウド更新＝PC不要）／オーナー: sukeaki.ito

# Pococha ランクボーダー早見表（自動公開ツール）

DeNA Creator Links で使う、Pococha のランク別ボーダー（メーター）の社内向けサイトと、その毎日自動更新の仕組み。**2ページ構成**：
- **早見表** … 指定日の全ランク・全締め時間のボーダー一覧
- **ボーダー推移** … 過去実績から「相場」を読む推移グラフ／予測レンジ／曜日別平均

## 公開URL
- 早見表：**https://dcl-events.github.io/pococha-borders/**
- ボーダー推移：**https://dcl-events.github.io/pococha-borders/trend/**

（GitHub Pages・Public。URLを知っているメンバー向けの社内利用。数値はもともと UPSTAR が公開しているもの。**両ページとも個人データは一切使わない**＝誰が見ても同じ「相場表」）

## 何ができる（早見表）
- **22時 / 24時 / 13時** の各締め時間ごとに、ランク別（S6〜E1）の到達ボーダーを一覧表示
- 列は Pococha の昇格段階に対応：**+2 / +1 / ±0**
  - **+2** … 2ランクアップに必要なメーター
  - **+1** … 1ランクアップに必要なメーター
  - **±0** … 現ランク維持に必要なメーター
- 上部の**日付ボタン→カレンダー**で過去日を選択（**2026-06-01 以降**を蓄積）。選んだ日の3締め時間をまとめて切替
- カレンダーの各日のドット＝データがある日
- 締め時間タブの右の**「📈 推移」**、または**各ランク行をクリック**で推移ページへ（見ていた締め時間・ランクを引き継ぐ）
- **毎朝10:00に自動更新**

## 何ができる（ボーダー推移）
過去のボーダー実績を貯めて、ランクごとの「相場」を読むためのページ。
- **ランク × 締め時間 × +2/+1/±0** を選んで、その系列の推移を表示
- **推移グラフ**：直近14日＋中央値ライン。チャットを**なぞる/タップ**すると小窓に日付・数値、**「早見表で見る →」ボタン**でその日の早見表へジャンプ（※タップだけでは飛ばない＝スマホ誤爆防止）
- **予測レンジ**：直近30日の最小〜中央値〜最大（「半分の日は◯〜◯万に収まる」）
- **曜日別平均**：各曜日の平均を実数付きで表示
- 右上の**「← 早見表」**で早見表へ戻る（締め時間を引き継ぐ）
- こちらも**毎朝10:00に自動更新**（データは早見表と共通の `history.json` を再利用）

## 反映タイミング（重要）
毎日10:00の取得時点で「確定している最新日」は締め時間で異なる：
- **22時・24時 → 前日（D-1）**
- **13時 → 前々日（D-2）**

そのためカレンダーで直近1〜2日は、締め時間によって空欄（「この日のデータはありません」）になることがある。これは仕様。過去日はすべて揃っている。

## データ出典
UPSTAR（`upstar.livestar.tokyo`）が公開している Firebase Cloud Function（認証不要の読み取り）。UPSTAR 側と数値は一致。

## 仕組み・保守（技術メモ）
- **更新はGitHubのクラウド（GitHub Actions）で完結＝PC不要**（2026-07-31クラウド化）。誰のMacも立ち上げなくても毎朝自動更新される
- 毎朝 **JST 10:00**（cron `0 1 * * *` UTC）に GitHub Actions `update.yml` が実行：
  取得（`fetch.py`）→ 早見表生成（`build.py`）→ **推移生成（`build_trend.py`）** → 自動コミット → Pages へ自動デプロイ
- リポジトリ：`github.com/dcl-events/pococha-borders`（Public）／ソースは `~/Claude/pococha-borders`（sukeaki の Mac）
- 全日付データをHTMLに埋め込む方式のため、日付切替はサーバ通信なしで一瞬
- 過去にもっと遡って貯めたい時：`backfill.py` の `START` を変えて一度だけ実行（日次運用では使わない）
- 今すぐ手動更新したい時：GitHub の Actions 画面 →「Daily update & deploy」→ Run workflow

## 注意 / 制約
- GitHub Actions の cron は混雑時に数分〜十数分ずれることがある（日次更新なので実用上問題なし）
- カレンダー下限は 2026-06-01 固定（それ以前を見たい場合は backfill で拡張が必要）
- ライブ配信の「時間ダイヤ」「配信時間」等とは別物。ここで扱うのは**ランクボーダー（メーター）**のみ
- 推移ページの「曜日別平均」や「月内サイクル」等の傾向は、蓄積が増えるほど精度が上がる“育つ指標”。データが少ないうちの断定は避ける（例：曜日差は実測でほぼ無し＝週末を狙う必要はない、が現時点の傾向）
